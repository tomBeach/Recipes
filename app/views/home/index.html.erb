<!-- ======= output ======= -->
<div id="output">

	<!-- ======= recipe title ======= -->
	<div class="recipeBox1">
		<h2 id="outputTitle">Home</h2>
	</div>


	<!-- ======= spacer ======= -->
	<div id="recipeBox2">
		<div class="halfBox">
		</div>
		<div class="halfBox">
		</div>
	</div>

	<!-- ======= instructions ======= -->
	<div id="welcomeText" class="middleBox">
		<p>Welcome to <span>My Recipes!</span></p>
		<p>You can add a new recipe line by line (using <span>Account > Type/Enter</span>), but it's easier to save your recipe as a text file.  Use this exact format for the file:</p>
		<p class="infoList">Recipe Title</p>
		<p class="infoList">ingredients</p>
		<p class="indented">list ingredients here</p>
		<p class="indented">one ingredient per line</p>
		<p class="infoList">instructions</p>
		<p class="indented">• List instructions here.</p>
		<p class="indented">• Add one instruction per line.</p>
		<p class="indented">• The bullets used here are optional; they do help readability though.</p>
		<p>Note that it's important to place and spell <i>ingredients</i> and <i>instructions</i> correctly.  (The program uses them to analyze the recipe.)  After editing and saving your file, click <span>My Account/Import Recipes</span>. Click the <span>Choose File</span> in the import window, then select the file and click <span>Okay</span>.</p>

	</div>

</div>
